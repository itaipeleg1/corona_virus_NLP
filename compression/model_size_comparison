def compare_model_sizes (model1, model2):
    # returning the size difference between the two models
    size1 = sum(p.numel() for p in model1.parameters())
    size2 = sum(p.numel() for p in model2.parameters())
    return size1 - size2    
